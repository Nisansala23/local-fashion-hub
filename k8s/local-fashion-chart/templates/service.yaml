apiVersion: v1
kind: Service
metadata:
  name: fashion-hub-service
  namespace: default
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 3000
      nodePort: {{ .Values.service.nodePort }}
  selector:
    app: fashion-hub
